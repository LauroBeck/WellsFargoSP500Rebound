/* * PROJECT: Stargate Node v2
 * ACTION: Sovereign DDL Materialization
 * TARGET: 7,172.23 Technical Reclamation
 */

CREATE TABLE SG26_RECALIBRATION_LOG (
    LOG_ID         NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CURRENT_INDEX  VARCHAR2(20) DEFAULT 'SP500',
    STATUS         VARCHAR2(50),
    TARGET_PX      NUMBER(10,2),
    VECTOR_SOURCE  VARCHAR2(100),
    LAST_UPDATE    TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE PORTFOLIO_POSITIONS (
    TICKER         VARCHAR2(10) PRIMARY KEY,
    ALLOCATION_PCT NUMBER(5,2),
    STRATEGY_TYPE  VARCHAR2(50)
);

-- Seed Initial 7,172.23 Inflection Data
INSERT INTO SG26_RECALIBRATION_LOG (STATUS, TARGET_PX, VECTOR_SOURCE)
VALUES ('READY_FOR_PIVOT', 7172.23, 'AMZN_ANTHROPIC_5GW');

INSERT INTO PORTFOLIO_POSITIONS (TICKER, ALLOCATION_PCT, STRATEGY_TYPE) VALUES ('DRAM', 25.00, 'VERTICAL_LOAD');
INSERT INTO PORTFOLIO_POSITIONS (TICKER, ALLOCATION_PCT, STRATEGY_TYPE) VALUES ('XLE', 5.00, 'STRUCTURAL_EXIT');

COMMIT;
